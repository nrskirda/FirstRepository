pipeline {
    agent any
    stages {
        stage('Build0') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        stage('Build1') {
            steps {
                sh 'pwd'
            }
        }
    }
}
